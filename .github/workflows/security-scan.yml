name: Security Scanner

on:
  workflow_call:
  workflow_dispatch: # Allows manual trigger

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: WordPress Scanner
        uses: 10up/wordpress-scanner-action@main
